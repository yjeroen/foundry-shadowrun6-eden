{{! Biography Tab }}
<section
  class='tab biography scrollable {{tab.cssClass}}'
  data-group='primary'
  data-tab='biography'
>

  <div class="section wide">
    <div class="section-head">
      <h2 class="section-title">{{systemFields.description.label}}</h2>
    </div>
    <div class="section-content">
      {{! Editors must receive enriched text data from getData to properly handle rolls, UUID links, etc. }}
      {{#if editMode}} 
        {{!-- No `toggled` attribute to keep prose-mirror active --}}
        <prose-mirror name="system.description" data-document-u-u-i-d="{{actor.uuid}}" value="{{system.description}}" collaborate>
          {{{enrichedDescription}}}
        </prose-mirror>
      {{else}}
        <p>{{{enrichedDescription}}}&nbsp;</p>
      {{/if}}
    </div>
  </div>
  
  <div class="section wide">
    <div class="section-head">
      <h2 class="section-title">{{systemFields.notes.label}}</h2>
    </div>
    <div class="section-content">
      {{! Editors must receive enriched text data from getData to properly handle rolls, UUID links, etc. }}
      {{#if editMode}} 
        {{!-- No `toggled` attribute to keep prose-mirror active --}}
        <prose-mirror name="system.notes" data-document-u-u-i-d="{{actor.uuid}}" value="{{system.notes}}" collaborate>
          {{{enrichedNotes}}}
        </prose-mirror>
      {{else}}
        <p>{{{enrichedNotes}}}&nbsp;</p>
      {{/if}}
    </div>
  </div>
  
</section>