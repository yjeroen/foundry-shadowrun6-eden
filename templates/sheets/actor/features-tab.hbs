{{! Features Tab }}
<section
  class='tab features scrollable {{tab.cssClass}}'
  data-group='primary'
  data-tab='features'
>
  <section class='grid grid-3col'>
    {{!-- <aside class='sidebar'>
      {{! The grid classes are defined in scss/global/_grid.scss. To use,
          use both the "grid" and "grid-Ncol" class where "N" can be any number
          from 1 to 12 and will create that number of columns.  }}
      <div class='abilities flexcol'>
        {{#each system.abilities as |ability key|}}
          <div class='ability flexrow flex-group-center'>
            <label
              class='resource-label rollable flexlarge align-left'
              data-action='roll'
              data-roll='d20+@abilities.{{key}}.mod'
              data-label='{{localize (lookup @root.config.abilities key)}}'
            >{{localize (lookup @root.config.abilities key)}}</label>
            <input
              type='text'
              name='system.abilities.{{key}}.value'
              value='{{ability.value}}'
              class='ability-score'
              data-dtype='Number'
            />
            <span
              class='ability-mod rollable'
              data-action='roll'
              data-roll='d20+@abilities.{{key}}.mod'
              data-label='{{localize (lookup @root.config.abilities key)}}'
            >{{numberFormat ability.mod decimals=0 sign=true}}</span>
          </div>
        {{/each}}
      </div>
    </aside> --}}

    {{! For the main features list, span the right two columns }}
    {{!-- <section class='main grid-span-2'>
      <ol class='items-list'>
        <li class='item flexrow items-header'>
          <div class='item-name'>{{localize 'Name'}}</div>
          <div class='item-controls'>
            {{#if @root.editMode}}
              <a
                class='item-control item-create'
                title='Create item'
                data-action='createDoc'
                data-document-class='Item'
                data-type='spritepower'
              >
                <i class='fas fa-plus'></i>
                {{localize 'DOCUMENT.New' type=(localize 'TYPES.Item.spritepower')}}
              </a>
            {{/if}}
          </div>
        </li>
        {{#each spritePowers as |item id|}}
          <li
            class='item flexrow'
            data-item-id='{{item._id}}'
            data-drag='true'
            data-document-class='Item'
          >
            <div class='item-name'>
              <div class='item-image'>
                <a class='rollable' data-roll-type='item' data-action='roll'>
                  <img
                    src='{{item.img}}'
                    title='{{item.name}}'
                    width='24'
                    height='24'
                  />
                </a>
              </div>
              <div>{{item.name}}</div>
            </div>
            <div class='item-controls'>
              <a
                class='item-control item-edit'
                title='{{localize "DOCUMENT.Update" type=(localize 'TYPES.Item.spritepower')}}'
                data-action='viewDoc'
              >
                <i class='fas fa-edit'></i>
              </a>
              {{#if @root.editMode}}
                <a
                  class='item-control item-delete'
                  title='{{localize "DOCUMENT.Delete" type=(localize 'TYPES.Item.spritepower')}}'
                  data-action='deleteDoc'
                >
                  <i class='fas fa-trash'></i>
                </a>
              {{/if}}
            </div>
          </li>
        {{/each}}
      </ol>
    </section>
  </section> --}}



{{#if (itemTypeInList actor.items 'spritepower')}}
<dt>{{ localize 'shadowrun6.section.spritepowers.title' }}</dt>
<dd>
	<!-- Sprite Powers -->
	{{#each (itemsOfType actor.items 'spritepower') as |item id|}} 
      <span class="item-span">
         <label class="item-desc" data-skill="{{id}}" data-item-id="{{item.id}}">{{item.name}}</label>
         <a class="item-control" data-uuid="{{item.uuid}}" title="Roll" data-action="roll" data-roll-type="complexform" data-item-id="{{item.id}}"><i class="fas fa-dice-six"></i></a>
         {{#unless @last}}<span>,&nbsp;</span>{{/unless}}
      </span>
   {{/each}}
</dd>

<!-- Sprite Powers -->
<div class="section" style="display: inline-block; width:100%; min-width: 362px;">
 <div class="section-head">
	<h2 class="section-title">{{ localize 'shadowrun6.section.spritepowers.title' }}</h2>
  <div class='item-controls'>
    {{#if @root.editMode}}
      <a
        class='item-control item-create'
        title='{{localize 'DOCUMENT.New' type=(localize 'TYPES.Item.spritepower')}}'
        data-action='createDoc'
        data-document-class='Item'
        data-type='spritepower'
      >
        <i class='fas fa-plus'></i>
      </a>
    {{/if}}
  </div>
</div>
<table>
  <thead>
    <tr>
      <th></th>
      <th style="text-align:left">{{ localize 'shadowrun6.label.column.name' }}</th>
      <th>{{ localize 'shadowrun6.section.spells.column.sustained' }}</th>
      <th colspan="2">{{ localize 'shadowrun6.label.column.action' }}</th>
    </tr>
  </thead>
		{{#each (itemsOfType actor.items 'spritepower') as |item id|}} 
		<tr class="item list-item draggable" data-item-id="{{item.id}}" data-uuid="{{item.uuid}}"
      data-item-id='{{item._id}}'
      data-drag='true'
      data-document-class='Item'
    >
      <td style="width:1rem"><button type="button" class={{#if (not item.system.description)}}"collapsible closed empty"{{else}}"collapsible closed" data-action='itemDescToggle'{{/if}}>+</button></td>
      <td>{{item.name}}</td>
      <td class="centered" style="width: 4em">
        {{#if (eq item.system.duration 'sustained')}}
        <label class="switch" title='{{localize "shadowrun6.section.sustained.toggle"}}'>
        <input type="checkbox" data-action='itemSustainToggle' {{checked item.system.isSustained}}>
        <span class="shadowrun6-slider round"></span>
        </label>
        {{/if}}
      </td>
      <td class="centered" style="width: 5em">
        <div class='item-controls'>
          <a class="item-control complexform-roll" data-item-id="{{item.id}}" data-uuid="{{item.uuid}}" title="Roll"><i class="fas fa-dice-six"></i></a>
          <a
            class='item-control item-edit'
            title='{{localize "DOCUMENT.Update" type=(localize 'TYPES.Item.spritepower')}}'
            data-action='viewDoc'
          >
            <i class='fas fa-edit'></i>
          </a>
          {{#if @root.editMode}}
            <a
              class='item-control item-delete'
              title='{{localize "DOCUMENT.Delete" type=(localize 'TYPES.Item.spritepower')}}'
              data-action='deleteDoc'
            >
              <i class='fas fa-trash'></i>
            </a>
          {{/if}}
        </div>
      </td>
    </tr>
    <tr>
      <td colspan="5">
        <div class="collapsible-content closed" style="font-size: 0.8em;">
          <div><strong>{{ localize 'shadowrun6.item.description' }}</strong></div>
          <div>{{{item.system.description}}}</div>
        </div>
      </td>
    </tr>
    {{/each}}
</table>
</div>
{{/if}}








</section>